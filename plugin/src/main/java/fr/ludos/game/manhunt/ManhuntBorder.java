package fr.ludos.game.manhunt;

import org.bukkit.Bukkit;
import org.bukkit.Location;
import org.bukkit.World;
import org.bukkit.WorldBorder;

public class ManhuntBorder {
//     private World world;
//     private WorldBorder border;

//     public ManhuntBorder(World world) {
//         this.world = world;
//         this.border = world.getWorldBorder();
//     }

//     public void setBorder(Location center, double size) {
//         border.setCenter(center);
//         border.setSize(size);
//     }

//     public void setBorder(double x, double z, double size) {
//         Location center = new Location(world, x, 0, z);
//         setBorder(center, size);
//     }

//     public void setBorder(double x, double y, double z, double size) {
//         Location center = new Location(world, x, y, z);
//         setBorder(center, size);
//     }

//     public void setBorder(double minX, double minZ, double maxX, double maxZ) {
//         double x = (minX + maxX) / 2;
//         double z = (minZ + maxZ) / 2;
//         double size = Math.max(maxX - minX, maxZ - minZ);
//         setBorder(x, z, size);
//     }

//     public void setBorder(double minX, double minY, double minZ, double maxX, double maxY, double maxZ) {
//         double x = (minX + maxX) / 2;
//         double y = (minY + maxY) / 2;
//         double z = (minZ + maxZ) / 2;
//         double size = Math.max(maxX - minX, Math.max(maxY - minY, maxZ - minZ));
//         setBorder(x, y, z, size);
//     }
}
